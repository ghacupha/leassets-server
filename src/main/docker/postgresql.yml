version: '2'
services:
  leassetsserver-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/leassetsServer/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=leassetsServer
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
